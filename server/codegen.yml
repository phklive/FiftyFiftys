overwrite: true
schema: 'http://localhost:4000/graphql'
documents: null
generates:
  src/generated/graphql.ts:
    config:
      contextType: './src/types.ts#MyContext'
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
